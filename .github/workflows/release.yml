name: Build Desktop Release

# Builds the double-click desktop bundles for macOS and Windows and attaches
# them to the GitHub Release. There's no cross-compilation: the .app must be
# built on a macOS runner and the .exe on a Windows runner, so we fan out over
# a runner matrix. Triggered by pushing a version tag (v1.2.3), or manually.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write  # needed to upload assets to the Release

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            script: bash packaging/build_macos.sh
            artifact: dist/GeneLens-*.zip
          - os: windows-latest
            script: packaging\build_windows.bat
            artifact: dist/GeneLens-*.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Smoke-test the suite before packaging
        run: pytest tests/ -q

      - name: Build bundle
        env:
          PYTHON: python
        run: ${{ matrix.script }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.os }}-bundle
          path: ${{ matrix.artifact }}

      - name: Extract changelog for this version
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          awk -v v="$VERSION" '/^## \[/{ if (p) exit; if (index($0, "[" v "]")) { p=1; next } } p' CHANGELOG.md > RELEASE_NOTES.md
          # Fall back to the tag name if that version has no changelog section.
          [ -s RELEASE_NOTES.md ] || echo "Gene Lens $GITHUB_REF_NAME" > RELEASE_NOTES.md

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: ${{ matrix.artifact }}
          body_path: RELEASE_NOTES.md
