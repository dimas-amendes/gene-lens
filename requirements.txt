# Core (pinned for reproducibility — bump via dependabot)
flask==3.1.3
tqdm==4.67.3
requests==2.34.2

# Translation (optional — install for PT-BR neural translation)
# argostranslate==1.9.6

# Local AI interpretation (optional — requires Ollama installed)
# ollama==0.3.3
