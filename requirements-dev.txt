# Development dependencies — install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Runtime is kept lean in requirements.txt; anything contributors need to
# run the test suite, lint, or develop locally goes here. Keeps the
# end-user install path light (Flask + Tqdm + Requests only).

pytest==9.1.1

# Desktop packaging — builds the double-click executable (see packaging/).
# Only needed to cut a release, not to run the test suite.
pyinstaller==6.11.1
# Pillow generates the Windows .ico app icon (packaging/icon.ico). The .icns is
# built natively with iconutil, so this is only for the Windows icon.
pillow==11.0.0
